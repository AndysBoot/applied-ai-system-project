# Copy this file to .env and fill in your own key to enable the RAG explanation
# feature (src/explain.py). Optional -- if left unset, the app still runs fine
# and falls back to the deterministic explanation for every recommendation.
ANTHROPIC_API_KEY=
